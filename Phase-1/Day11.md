# Other Data Types

## Sets
* Set is unordered collection of immutable
* Set is mutable
* Set does not supports duplicates
* set is hashable
```py
empty_set = set()
num = {1,2,3,4}
set.add(5)
set.add(4) # wont add anything
set.remove(3)
```
### Set Operations
1. & (Intersection)
